const Flights = () => {
  return (
    <div>
      <h2 className="text-2xl font-bold">Flight List</h2>
      {/* Add logic for flight management (listing, adding, etc.) */}
      <p>List of flights will be displayed here.</p>
    </div>
  );
};

export default Flights;
